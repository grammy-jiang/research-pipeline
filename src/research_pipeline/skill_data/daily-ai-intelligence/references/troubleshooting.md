# Troubleshooting

- Empty report: check that sources are enabled and within Phase A access methods.
- Validation failure: inspect `validation/validation.json`.
- Duplicate items: inspect `clusters/clusters.jsonl` and source dedup keys.
- Obsidian export refused: the target note likely exists without matching
  generated frontmatter.
